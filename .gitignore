# Sensitive files
config/gh/hosts.yml
ssh/id_*
ssh/*.pub
.secrets

# sops — track encrypted files, never plaintext
# secrets/*.yaml is encrypted by sops and safe to commit
# The age private key must NEVER be committed
config/sops/age/keys.txt

# OS files
.DS_Store
**/.DS_Store

# Editor files
*.swp
*.swo
*~
